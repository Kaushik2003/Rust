pub enum Status {
    CONNECTED,
    INTERRUPTED,
}
pub fn connect_to_database() -> Status{
    //connect to database
    Status::CONNECTED
}

pub fn get_user() {
    //fetch the user from db and return
}